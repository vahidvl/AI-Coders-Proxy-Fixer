# 🚀 Marketing & Promotion Copy

Use these pre-written posts to easily promote **AI Coders Proxy Fixer** and get those GitHub stars! 

---

## 1. Reddit (For r/vscode, r/Cursor, r/ClaudeAI)
**Title:** I got tired of Claude Code & AI IDEs failing behind corporate proxies, so I built a 1-click bypass GUI 🚀

**Body:**
> Hey everyone,
>
> If you're working behind a strict corporate firewall, or live in a region with heavy geoblocking, you've probably seen errors like `failed to check terminal shell support` in VS Code/Antigravity or `ETIMEDOUT` / `Connection Refused` in Cursor and Claude Code.
>
> Standard VPNs and SOCKS5 proxies often break because local Language Servers get routed incorrectly.
> 
> I got tired of constantly editing `settings.json` and wrestling with Windows Environment Variables, so I built an open-source, portable Windows GUI that does it automatically. It injects the correct proxy settings into your IDEs while actively bypassing `localhost` to keep your internal servers from crashing.
> 
> It's completely free and open-source. Hope it saves someone a headache!
> 
> **Repo & Download:** [Insert Your GitHub Link Here]
> 
> *(If it helps you, a GitHub star ⭐ would be amazing!)*

---

## 2. Twitter / X (Persian Community - توییتر فارسی)
**متن توییت:**
خسته شده بودم از بس وسط کد زدن با Cursor و Antigravity ارور `User location is not supported` یا `EOF` کلود کد می‌گرفتم. تحریم‌ها و فیلترینگ حسابی جلوی پروکسی شدن لوکال سرورها رو می‌گیره.

برای همین یه ابزار اپن‌سورس خیلی سبک (GUI) ساختم که با یک کلیک کل تنظیمات رجیستری، محیط ویندوز و settings.json ادیتورها رو درست می‌کنه و به راحتی از تحریم رد میشید! 🚀 

بدون نیاز به نصب، فقط اجراش کنید.
اگر به دردتون خورد ریپازیتوری رو استار بدید:
[لینک گیت‌هاب شما]
#برنامه_نویسی #هوش_مصنوعی #AI #Cursor #Claude #تحریم

---

## 3. GitHub Issue Reply Template
*Find open issues complaining about `failed to check terminal shell support` or `Cloud Code EOF` and reply with this:*

> Hey @[username], I was running into this exact same proxy timeout / routing issue on my machine. 
> 
> The problem usually happens because global `HTTP_PROXY` variables accidentally route `localhost` traffic through the proxy, crashing the local language server. I built a tiny open-source portable GUI tool that automatically patches the `settings.json` with the correct `http.noProxy` bypass arrays and toggles the shell integration fix.
> 
> It might solve your issue instantly. You can grab the `.exe` or source code here: [Insert Your GitHub Link Here] 
> Hope this helps! 
